{
  "buildCommand": "pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py seed_centers && python manage.py collectstatic --noinput",
  "outputDirectory": "staticfiles",
  "installCommand": "pip install -r requirements.txt",
  "framework": null,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/admin/(.*)",
      "dest": "api/index.py"
    },
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
