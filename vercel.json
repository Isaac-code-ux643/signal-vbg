{
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "python manage.py migrate --noinput && python manage.py seed_centers && python manage.py create_admin && python manage.py collectstatic --noinput",
  "framework": null,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/admin/(.*)",
      "dest": "api/index.py"
    },
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
